import 'package:flutter/material.dart';

class EnablingSplashScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("..."),
      ),
      body: new Container(),
    );
  }
}